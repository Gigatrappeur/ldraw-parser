{
  "name": "ldraw-parser",
  "version": "1.0.0-beta",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build ./src/index.ts ./src/cli.ts --outdir=dist --target=bun --splitting --minify-syntax --minify-whitespace"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@types/node": "^25.6.0"
  }
}
